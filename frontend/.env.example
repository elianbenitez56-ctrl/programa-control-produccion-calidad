# ===== SIGPC Frontend - Variables de entorno =====
# URL base de la API del backend (FastAPI + prefix /api/v1)
# En Render debe ser la URL HTTPS pública del backend, definida antes de npm run build.
VITE_API_BASE_URL=http://localhost:8000/api/v1
